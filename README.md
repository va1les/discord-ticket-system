# Установка

> **1.** Заполните файл `config.json`<br>
> `token` — Токен бота.<br>
> `prefix` — Префикс бота.<br>
> `mongo_uri` — Ссылка на подключение к базе данных MongoDB.

> **2.** Установите все модули — `npm i`

> **3.** Запустите бота — `node .`.
***
# Система
`!setup #channel @role`
> В указанный канал отправится сообщение с кнопкой создание Тикета.

`!block @user`
> Заблокирует пользователю создавать тикеты. (от спама)

`!unblock @user`
> Разблокирует пользоватателю создавать тикеты.